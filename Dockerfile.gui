# GUI Dockerfile
FROM node:20-alpine

WORKDIR /app

# Install dependencies first for better caching
COPY gui/package*.json ./
RUN npm install

# Copy source code
COPY gui/ .

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
