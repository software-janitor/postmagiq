# GPU override for Ollama service
# Auto-applied when nvidia-docker is detected

services:
  ollama:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
